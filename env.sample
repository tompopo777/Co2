# Django settings
DEBUG=True                          # Set to False in production
SECRET_KEY='replace-me-in-production'

# Allowed hosts (comma-separated, e.g. 127.0.0.1,yourdomain.com)
SERVER=127.0.0.1

# MariaDB database settings
DB_NAME=co2
DB_USERNAME=root
DB_PASS='your_password'
DB_HOST=127.0.0.1
DB_PORT=3306
